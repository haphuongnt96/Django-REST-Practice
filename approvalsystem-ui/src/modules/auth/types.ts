// eslint-disable-next-line @typescript-eslint/no-unused-vars
declare namespace Auth {
  type User = {
    //email?? usernameのまちがい？
    email: string
    password: string
    access: string
    refresh: string
  }
}
